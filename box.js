
function weather(temp, wind) {
  console.log( `Its is ${temp}  and the wind chill is ${wind}`)
}

weather(60,50)

function say(greeting, firstName) {
  return `${greeting}, ${firstName}!`
}

function add(x, y) {
  return x + y
}
